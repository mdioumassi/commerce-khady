{% extends 'base.html.twig' %}

{% block title %}Nos tresses
{% endblock %}

{% block body %}
 	<div style="background:url(https://picsum.photos/500/500); no-repeat" class="py-5 text-white text-center">
    <h1>My First Bootstrap 5 Page</h1>
    <p>Resize this responsive page to see the effect!</p> 
    </div>
    {% include 'shared/_navbar.html.twig' %}
	<div class="container">
		<h1>Nos tresses</h1>
		<div class="row">
			{% for product in products %}
				<div class="col-sm-3 mb-4">
					<div class="card">
						<img src="https://picsum.photos/200/200" class="img-fluid" alt="">
						<div class="card-body">
							<h4 class="cartd-title">{{ product.name }}</h4>
							<p class="card-text">
								{{ product.description }}
							</p>
							<a href="{{path('app.tresse.show', {'slug':product.slug})}}" class="btn btn-primary btn-sm">Détails</a>
							<a href="#" class="btn btn-success btn-sm">Ajouter au panier</a>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}
